package com.example.applyricspr.repository

import com.example.applyricspr.data.*

/*
class LyricsRepository(private val dao: SongDao) {
    suspend fun fetchLyrics(artist: String, title: String): LyricsResponse {
        return RetrofitInstance.api.getLyrics(title, artist)
    }

    suspend fun saveSong(song: SongEntity) = dao.insertSong(song)
    suspend fun getSavedSongs() = dao.getAllSongs()
    suspend fun deleteSong(song: SongEntity) = dao.deleteSong(song)
} */